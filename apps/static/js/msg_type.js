/**
 * MyExtension MSG_TYPE
 */
const MSG_TYPE = {
    ////////////////////如下是popup中的menu，value和filename相同///////////////////
    OPTIONS:'options'
};

(typeof module === 'object') ? module.exports = MSG_TYPE : '';
